# Fix Session Stats Background
Themes (like Hazy) make it difficult to read text due to transparent background

```css
#sesh-stats-overlay{--spice-card: rgba(var(--spice-rgb-card), 1) !important}
```
